{% if content is defined %}
    <a href="{{ path('ez_urlalias', {locationId: content.contentInfo.mainLocationId}) }}" class="btn btn-secondary btn-lg btn-block">{{ ez_content_name(content) }}</a>
{% endif %}
